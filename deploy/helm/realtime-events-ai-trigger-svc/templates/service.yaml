apiVersion: v1
kind: Service
metadata:
  name: {{ include "realtime-events-ai-trigger-svc.fullname" . }}
  labels:
    {{- include "realtime-events-ai-trigger-svc.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "realtime-events-ai-trigger-svc.selectorLabels" . | nindent 4 }}
