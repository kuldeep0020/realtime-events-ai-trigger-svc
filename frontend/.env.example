# Backend API base URL. When empty (default), the frontend uses mock data from /mocks/*.json.
# Set this to the realtime-events-ai-trigger-svc service URL for live mode.
# Example: http://localhost:8080
NEXT_PUBLIC_API_BASE=
