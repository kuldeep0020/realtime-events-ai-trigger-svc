# Backend API base URL (scheme + host, no trailing slash). When empty, the frontend uses mock data from /mocks/*.json.
# Local backend: http://localhost:8080
# Dev deploy (matches ingress on realtime-events-ai-trigger-api): https://realtime-events-ai-trigger-api.dev-rudder.rudderlabs.com
NEXT_PUBLIC_API_BASE=
